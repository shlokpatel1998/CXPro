# Ralph Progress Log
Started: PRD-9 refactor — attempt 2, re-running on top of merged PRD-8 (main @ f0153b1)

## Codebase Patterns
- main now contains PRD-8 product changes: 6-role enum (OCA/CM/cx_engineer/field_technician/design_engineer/owner_fm), migration 017, frontend lib/roles + lib/permissions, ProjectSwitcher, Name column on members table, Manage Team button on /projects rows
- The refactor must PRESERVE all of PRD-8's behavior — pure restructure only. After US-001..US-008, every PRD-8 user-visible feature must still work identically.
- attempt-1 ran on a pre-PRD-8 base and would have silently dropped PRD-8 product changes; archived at scripts/ralph/archive/2026-05-27-refactor-attempt-1/ and tagged refactor-attempt-1-pre-prd8 in git.
- Backend tests baseline before refactor: capture pytest pass count first, every story must match it.
- Frontend tests baseline before refactor: capture vitest pass count first.
- Use raw asyncpg for database operations (no Supabase SDK).
- Use vitest imports (describe, test, expect) in frontend test files.
- Frontend test files go in src/lib/__tests__/ or colocated with deep modules.
- Type-check command: npm run type-check (not typecheck).
- Backend pytest baseline (before refactor): 37 passed, 5 failed (pre-existing failures: test_redeem_pending_invitations, test_invitation_service*, tests/test_invites_endpoint::test_cm_can_invite, tests/test_invites_endpoint::test_oca_can_still_invite)
- auth.py imports can_manage_team from permissions.py to avoid a parallel enforcement path
- outbox table schema: INSERT INTO outbox (event_type, event_data) VALUES ($1, $2)

## 2026-05-27 - US-001
- Created backend/db.py: get_supabase_client() and get_db_connection() factories
- Created backend/auth.py: security (HTTPBearer), get_current_user(), require_permission() delegating to permissions.py
- Created backend/outbox.py: emit_event(conn, event_type, event_data) single public function
- Updated backend/main.py: removed inline Supabase/asyncpg setup and get_current_user, now imports from db.py and auth.py
- permissions.py unchanged — keeps can_manage_team/can_create_project as administrative predicates; auth.py delegates to it (no parallel enforcement path)
- Files changed: backend/db.py (new), backend/auth.py (new), backend/outbox.py (new), backend/main.py (updated)
- pytest: 37 passed, 5 failed — identical to baseline
- **Learnings for future iterations:**
  - The 5 pre-existing test failures are: test_redeem_pending_invitations (unique constraint on live DB), test_invitation_service* (live DB KeyError), tests/test_invites_endpoint::test_cm/oca (mock side_effect exhausted before all 3 fetchrow calls)
  - invitation_service.py still imports from permissions.py directly — US-002 will move invitation logic to contexts/identity_access/invitations.py
  - contexts/__init__.py and contexts/identity_access/__init__.py don't exist yet as .py files (only __pycache__) — US-002 must create them
---

## 2026-05-27 - US-002
- Created backend/contexts/__init__.py and backend/contexts/identity_access/__init__.py
- Created backend/contexts/identity_access/invitations.py: inlines InviteDecision pure logic (_decide_invite_action, ExistingInvite, action dataclasses) and contains InvitationService class plus send_invite() public function
- Deleted backend/invitation_service.py and backend/invite_decision.py (no shims)
- Updated backend/main.py: imports InvitationService from contexts.identity_access.invitations
- Updated tests: test_invite_decision.py, test_invitation_service.py, test_invitation_service_simple.py, tests/test_invites_endpoint.py — all import paths updated; assertions unchanged
- pytest: 37 passed, 5 failed — identical to US-001 baseline
- **Learnings for future iterations:**
  - InviteDecision functions are private (_decide_invite_action) inside invitations.py; test_invite_decision.py imports them with alias `as decide_invite_action`
  - contexts/identity_access/ is now a proper Python package with __init__.py files
  - roles.py and permissions.py still exist at backend root — US-003 will move roles.py and admin side of permissions.py into memberships.py
---

## 2026-05-27 - US-003
- Created backend/contexts/identity_access/memberships.py: absorbs ROLES, ROLE_LABELS, is_valid_role from roles.py and can_manage_team, can_create_project from permissions.py
- Deleted backend/roles.py and backend/permissions.py (no shims)
- Updated backend/auth.py: import can_manage_team from contexts.identity_access.memberships instead of permissions
- Updated backend/contexts/identity_access/invitations.py: inline imports of is_valid_role and can_manage_team now come from contexts.identity_access.memberships
- Updated tests/test_roles.py and tests/test_permissions.py: import paths updated to contexts.identity_access.memberships; assertions unchanged
- pytest: 37 passed, 5 failed — identical to US-002 baseline
- **Learnings for future iterations:**
  - memberships.py is now the single source of truth for roles and permission predicates
  - auth.py still has the require_permission enforcement helper using can_manage_team from memberships
  - invitations.py uses lazy inline import (inside create_invitation method) for memberships — consistent with prior pattern
  - US-004 will create contexts/identity_access/api.py router and slim down main.py to a composition root
---

## 2026-05-27 - US-004
- Created backend/contexts/identity_access/api.py: thin APIRouter with POST /invites handler; delegates to InvitationService from invitations.py; uses get_current_user, get_supabase_client, get_db_connection from deep modules
- Updated backend/main.py: stripped all route handlers and business logic; now only FastAPI instantiation, CORS middleware, exception handler, include_router(identity_access_router), and /health + / utility endpoints
- Files changed: backend/contexts/identity_access/api.py (new), backend/main.py (updated)
- pytest: 37 passed, 5 failed — identical to US-003 baseline
- **Learnings for future iterations:**
  - InvitationRequest Pydantic model moved from main.py into api.py — test_main.py (TestClient) still passes because it imports `app` from main, and app now includes the router
  - main.py exception_handler and CORS middleware remain in main.py (app-level concerns, not context-specific)
  - US-005 will relocate workers to backend/workers/
---

## 2026-05-27 - US-005
- Created backend/workers/__init__.py
- Moved backend/outbox_dispatcher.py → backend/workers/outbox_dispatcher.py (verbatim, no internal imports to update — self-contained)
- Moved backend/inbox_subscriber.py → backend/workers/inbox_subscriber.py (updated `from outbox_dispatcher` → `from workers.outbox_dispatcher`)
- Moved backend/test_outbox_dispatcher.py → backend/workers/test_outbox_dispatcher.py (updated import path)
- Moved backend/verify_outbox_dispatcher.py → backend/workers/verify_outbox_dispatcher.py (no imports to update)
- Updated backend/cx_execution_agent.py: lazy import `from outbox_dispatcher` → `from workers.outbox_dispatcher`
- Updated backend/pytest.ini: `--ignore=test_outbox_dispatcher.py` → `--ignore=workers/test_outbox_dispatcher.py`
- pytest: 37 passed, 5 failed — identical to US-004 baseline
- **Learnings for future iterations:**
  - railway.json start command is `uvicorn main:app` — no worker process entry points referenced, so no deploy config update needed
  - apply_outbox_migration.py references a migration SQL path string (not an import), so no import update needed
  - test_outbox_dispatcher.py is already ignored in pytest.ini (requires live DB); only needed to update the --ignore path
  - US-006 starts frontend consolidation: lib/{members,identity,roles,permissions}.ts → contexts/identity/api.ts
---

## 2026-05-27 - US-006
- Created frontend/src/contexts/identity/api.ts: merges ROLES/ROLE_LABELS/isValidRole from roles.ts, canManageTeam/canCreateProject from permissions.ts, getInitials from identity.ts, and all members.ts functions (getMembersForProject, getPendingInvitesForProject, getCurrentUserRole, updateDiscipline) plus ProjectMember/PendingInvite interfaces
- Deleted frontend/src/lib/{members,identity,roles,permissions}.ts (no shims)
- Moved test files: lib/__tests__/{members,identity,roles,permissions}.test.ts → contexts/identity/{members,identity,roles,permissions}.test.ts with import paths updated to ./api; assertions unchanged
- Updated consumers: app/(app)/project/[id]/members/page.tsx, project/[id]/page.tsx, projects/page.tsx, entity/[id]/page.tsx, components/UserMenu.tsx
- Updated lib/supabase.ts: changed `import type { Role } from './roles'` → `import type { Role } from '@/contexts/identity/api'`
- Fixed pre-existing isOCA undefined variable in members/page.tsx → replaced with canManage (existing state variable)
- Added vi.mock('@/lib/supabase') to identity/roles/permissions test files to prevent supabase init error at module load
- Typecheck passes; vitest: 4 failed (2 dashboard pre-existing + 2 members getPendingInvites pre-existing), 66 passed
- **Learnings for future iterations:**
  - When tests import from a module that imports supabase at module load time, must mock supabase even if the test doesn't use DB functions
  - contexts/identity/api.ts imports supabase at top level; pure-function tests (getInitials, roles, permissions) need vi.mock('@/lib/supabase') to avoid init error
  - The 2 getPendingInvitesForProject failures are pre-existing (toEqual assertion missing send_count field) — preserved as-is per "assertions unchanged" requirement
  - US-007 will move UserMenu.tsx → contexts/identity/ui.tsx
---

## 2026-05-27 - US-007
- Created frontend/src/contexts/identity/ui.tsx: exports UserMenu as named export (moved verbatim from components/UserMenu.tsx)
- Deleted frontend/src/components/UserMenu.tsx (no shim)
- Updated frontend/src/app/(app)/layout.tsx: changed `import UserMenu from "@/components/UserMenu"` to `import { UserMenu } from "@/contexts/identity/ui"`
- Created frontend/src/contexts/identity/ui.test.tsx: colocated test asserting UserMenu is exported (no pre-existing tests to migrate)
- Typecheck passes; vitest: 4 failed (same 2 dashboard + 2 members pre-existing failures), 67 passed (one new test added)
- **Learnings for future iterations:**
  - UserMenu exported as named export in ui.tsx (not default); consumers must use `{ UserMenu }` destructuring
  - No other Identity-only components existed beyond UserMenu — cross-cutting components (CommandPalette, UploadModal, etc.) correctly left in place
  - US-008 will delete the empty frontend/src/contexts/commissioning/ stub
---

## 2026-05-27 - US-008
- Pre-deletion grep for 'contexts/commissioning' in frontend/src/ returned zero hits — safe to delete
- Deleted frontend/src/contexts/commissioning/index.ts and types.ts (directory removed entirely)
- Typecheck passes; vitest: 4 failed (same pre-existing failures), 67 passed — identical to US-007 baseline
- **Learnings for future iterations:**
  - All US-001..US-008 stories now pass — refactor pilot complete
  - The bounded-context layout (deep modules, Supabase-pragmatic) is fully established: backend/contexts/identity_access/ and frontend/src/contexts/identity/ are the canonical reference implementations
---
